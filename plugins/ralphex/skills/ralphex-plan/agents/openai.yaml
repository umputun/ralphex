interface:
  display_name: "Ralphex Plan"
  short_description: "Create an executable plan through guided discovery"
  default_prompt: "Use $ralphex-plan to explore this request and create a Ralphex implementation plan."

policy:
  allow_implicit_invocation: true
